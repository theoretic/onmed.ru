#ui-swatches{
padding:.2rem;
display:flex;
}

a.swatch{
	width:2rem;
	height:2rem;
	margin:.3rem;
	.rounded(4rem);
	//box-shadow: 0 0 .1rem rgba(0,0,0,.5);
	//.transition();

	&:hover{
		//width:2.1rem;
		//height:2.1rem;
		//margin:.1rem;
		cursor:pointer;
		box-shadow: 0 0 1rem rgba(0,0,0,.5);
	}

	&.selected{
	width:3rem;
	height:3rem;
	box-shadow: 0 0 1rem rgba(0,0,0,.2);
	}
}

